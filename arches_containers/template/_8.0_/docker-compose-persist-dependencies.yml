
services:
    nginx-{{project_urlsafe}}-{{project_hash}}:
      container_name: nginx-{{project_urlsafe}}-{{project_hash}}
      image: nginx:latest
      ports:
        - "8080:80"
      volumes:
        - ./docker/nginx-config/default.conf:/etc/nginx/conf.d/default.conf
      networks:
        - {{project_urlsafe}}-{{project_hash}}-network

    db-{{project_urlsafe}}-{{project_hash}}:
      container_name: db-{{project_urlsafe}}-{{project_hash}}
      image: postgis/postgis:16-3.5
      volumes:
        - ../../{{project}}-postgres-data:/var/lib/postgresql/data
        - ../../{{project}}-postgres-log:/var/log/postgresql
        - ./docker/init-unix.sql:/docker-entrypoint-initdb.d/init.sql # to set up the DB template
      ports:
        - '5433:5432'
      env_file: 
        - ./docker/env_file.env
      networks:
        - {{project_urlsafe}}-{{project_hash}}-network

    elastic-bindmount-init-{{project_urlsafe}}-{{project_hash}}:
      image: alpine
      container_name: elastic-bindmount-init-{{project_urlsafe}}-{{project_hash}}
      restart: "no"
      entrypoint: |
        /bin/sh -c "chown 1000:1000 /esBindMount"
      volumes:
        - ../../{{project}}-elasticsearch-data:/esBindMount


    elasticsearch-{{project_urlsafe}}-{{project_hash}}:
      container_name: elasticsearch-{{project_urlsafe}}-{{project_hash}}
      image: docker.elastic.co/elasticsearch/elasticsearch:8.19.4
      #platform: linux/arm64
      volumes:
        - ../../{{project}}-elasticsearch-data:/usr/share/elasticsearch/data
      ports:
        - "9201:9200"
        - "9301:9300"
      env_file: 
        - ./docker/env_file.env
      networks:
        - {{project_urlsafe}}-{{project_hash}}-network
      depends_on:
        - elastic-bindmount-init-{{project_urlsafe}}-{{project_hash}}

    rabbitmq-{{project_urlsafe}}-{{project_hash}}:
      container_name: rabbitmq-{{project_urlsafe}}-{{project_hash}}
      image: rabbitmq:4.1-management
      hostname: my-rabbit
      volumes:
        - rabbitmq-data-{{project_hash}}:/var/lib/rabbitmq/mnesia/rabbit@my-rabbit
        - rabbitmq-logs-{{project_hash}}:/var/log/rabbitmq/log
      ports:
        - 5673:5672
        - 15673:15672
      networks:
        - {{project_urlsafe}}-{{project_hash}}-network

    #pg-feat-{{project_urlsafe}}:
    #  container_name: pg-feat-{{project_urlsafe}}
    #  image: pramsey/pg_featureserv
    #  volumes:
    #    - ../../pg_featureserv_config_{{project}}:/config
    #  environment:
    #    - DATABASE_URL=postgresql://arches_spatial_views:arches_spatial_views@db-{{project_urlsafe}}:5432/{{project}}
    #  ports:
    #    - 9000:9000
    #  networks:
    #    - {{project_urlsafe}}-network
    #  depends_on:
    #    - db-{{project_urlsafe}}
    
    #pg-tile-{{project_urlsafe}}:
    #  container_name: pg-tile-{{project_urlsafe}}
    #  image: pramsey/pg_tileserv
    #  volumes:
    #    - ../../pg_tileserv_config_{{project}}:/config
    #  environment:
    #    - DATABASE_URL=postgresql://arches_spatial_views:arches_spatial_views@db-{{project_urlsafe}}:5432/{{project}}
    #  ports:
    #    - 7800:7800
    #  networks:
    #    - {{project_urlsafe}}-network
    #  depends_on:
    #    - db-{{project_urlsafe}}


networks:
  {{project_urlsafe}}-{{project_hash}}-network:
    name: {{project_urlsafe}}-{{project_hash}}-network
    driver: bridge

volumes:
    rabbitmq-logs-{{project_hash}}:
    rabbitmq-data-{{project_hash}}:
