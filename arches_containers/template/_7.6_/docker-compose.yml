
services:
    {{project_urlsafe}}-{{project_hash}}:
      container_name: {{project_urlsafe}}-{{project_hash}}
      image: he/{{project}}-{{project_hash}}:dev_build
      build:
        args:
          - "PROJ_NAME={{project}}"
          - "PROJ_REPO_NAME={{project_repo_directory}}"
          - "DOCKER_PATH=./.arches_containers/{{project}}/docker"
        context: ../..
        dockerfile: ./.arches_containers/{{project}}/Dockerfile
        target: dev
      command: run_arches
      volumes:
        - ../../arches:/web_root/arches
        - ../../{{project_repo_directory}}:/web_root/{{project}}
        - ./docker:/web_root/docker
        - ../../{{project_repo_directory}}-package:/web_root/{{project}}_package
        - ../../{{project_repo_directory}}-data:/web_root/{{project}}_data
      env_file:
        - ./docker/env_file.env
      ports:
        - 8002:8000
        - 5678:5678
      stdin_open: true
      tty: true
    
    ## - possible 
    #{{project_urlsafe}}-bash:
    #  profiles:
    #    - livereload
    #  container_name: {{project_urlsafe}}-bash
    #  image: he/{{project}}:dev_build
    #  command: ["bash"]
    #  volumes:
    #    - ../../arches:/web_root/arches
    #    - ../../{{project_repo_directory}}:/web_root/{{project}}
    #    - ./docker:/web_root/docker
    #    - ../../{{project_repo_directory}}-package:/web_root/{{project}}_package
    #    - ../../{{project_repo_directory}}-data:/web_root/{{project}}_data
    #  env_file:
    #    - ./docker/env_file.env
    #  ports:
    #    - 35729:35729      
    #  depends_on:
    #      - {{project_urlsafe}}-{{project_hash}}
    #  stdin_open: true
    #  tty: true

    {{project_urlsafe}}-{{project_hash}}-livereload:
      profiles:
        - livereload
      container_name: {{project_urlsafe}}-{{project_hash}}-livereload
      image: he/{{project}}-{{project_hash}}:dev_build
      command: run_livereload
      volumes:
        - ../../arches:/web_root/arches
        - ../../{{project_repo_directory}}:/web_root/{{project}}
      env_file:
        - ./docker/env_file.env
      ports:
        - 35729:35729      
      depends_on:
          - {{project_urlsafe}}-{{project_hash}}
      stdin_open: true
      tty: true
    
    {{project_urlsafe}}-{{project_hash}}-webpack:
      container_name: {{project_urlsafe}}-{{project_hash}}-webpack
      image: he/{{project}}-{{project_hash}}:dev_build
      command: run_webpack
      volumes:
        - ../../arches:/web_root/arches
        - ../../{{project_repo_directory}}:/web_root/{{project}}
      env_file:
        - ./docker/env_file.env
      ports:
        - 8028:8021
      depends_on:
          - {{project_urlsafe}}-{{project_hash}}
      stdin_open: true
      tty: true

networks:
  default:
    external: true
    name: {{project_urlsafe}}-{{project_hash}}-network

volumes:
    arches-log-{{project_hash}}:
    arches-static-{{project_hash}}:
